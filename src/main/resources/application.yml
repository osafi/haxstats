spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/haxstats
    username: haxstats
    password: haxstats
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
logging:
  level:
    org.hibernate.type: trace
